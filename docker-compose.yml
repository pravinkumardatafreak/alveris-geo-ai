services:
  alveris-cockpit:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: alveris-decision-cockpit
    ports:
      - "8501:8501"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./data:/app/data
    restart: unless-stopped
